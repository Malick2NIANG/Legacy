/**
 * Indicateur de progression d'une expérience en cours.
 * Affiche la barre de progression, les logs temps-réel et l'état Celery.
 */
import React from 'react'

function ExperimentStatus({ experimentId, status, progress }) {
  return (
    <div className="experiment-status">
      {/* Barre de progression en % */}
      {/* Statut textuel (En attente / En cours / Terminé) */}
      {/* Fenêtre de logs (optionnel) */}
    </div>
  )
}

export default ExperimentStatus
