/**
 * Carte d'affichage d'une expérience ML.
 * Montre le nom, le dataset utilisé, le modèle, la date et le statut (pending/running/done/failed).
 */
import React from 'react'

function ExperimentCard({ experiment, onView, onStop }) {
  return (
    <div className="experiment-card">
      {/* Nom et identifiant */}
      {/* Dataset et modèle associés */}
      {/* Badge de statut coloré */}
      {/* Date de lancement */}
      {/* Boutons actions */}
    </div>
  )
}

export default ExperimentCard
