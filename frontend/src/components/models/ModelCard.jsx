/**
 * Carte d'affichage d'un modèle ML.
 * Montre le nom, le type d'algorithme, les hyperparamètres et le statut.
 */
import React from 'react'

function ModelCard({ model, onEdit, onDelete }) {
  return (
    <div className="model-card">
      {/* Nom et type du modèle */}
      {/* Hyperparamètres résumés */}
      {/* Statut (entraîné / brouillon) */}
      {/* Boutons actions */}
    </div>
  )
}

export default ModelCard
