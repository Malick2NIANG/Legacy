/**
 * Liste des modèles ML enregistrés.
 * Gère la recherche par nom/type et l'affichage des ModelCard.
 */
import React from 'react'
import ModelCard from './ModelCard'

function ModelList({ models, loading }) {
  return (
    <div className="model-list">
      {/* Filtres par type d'algorithme */}
      {/* Grille de ModelCard */}
    </div>
  )
}

export default ModelList
